{
  "AppSettings": {
    "Secret": "THIS IS USED TO SIGN AND VERIFY JWT TOKENS, REPLACE IT WITH YOUR OWN SECRET, IT CAN BE ANY STRING",
    "NoReplyEMailHost": "smtp.1und1.de",
    "NoReplyEMailPort": 587,
    "NoReplyEMailAddress": "noreply@shopping-now.net",
    "NoReplyEMailPassword": "********",
    "DbServerAddress": "server=localhost;", // Is used if IsDocker == False, uses the local mysql server
    "DbServerAddressDocker": "server=db;port=3306;", // Is used if IsDocker == True instead of DbServerAddress
    "DbName": "shoppinglistserver",
    "DbUser": "ShoppingListDBUser",
    "DbPassword": "mysql-password-1234",
    "UseHttpsRedirect": "False", // If you want to force https. Should be enabled in production on the external server.    
    "UseDocker": "False" // In docker mode, DbServerAddressDocker will be chosen
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  "AllowedHosts": "*",
  // ssl support, see https://dev.to/___bn___/free-certified-ssl-certificate-in-asp-net-5-kestrel-application-kgn
  // The files in the project folder are self signed certificates. They were generated with:
  // openssl req -nodes -x509 -newkey rsa:2048 -keyout privkey.pem -out fullchain.pem -days 999999
  // They are really only here as placeholders. If you use a local server, connect to it via http or ignore the certificate check.
  // On server side, real certificates (e.g. generated by certbot) should be used.
  "Kestrel": {
    "Certificates": {
      "Default": {
        "Path": "cert-files/fullchain.pem",
        "KeyPath": "cert-files/privkey.pem"
      }
    }
  }
}
